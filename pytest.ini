[pytest]
asyncio_mode = auto
testpaths = tests
# Source modules live at the repo root, tests in tests/. Put the root on
# sys.path so imports resolve regardless of how pytest is invoked (`pytest`
# console script in CI vs `python -m pytest` locally).
pythonpath = .
